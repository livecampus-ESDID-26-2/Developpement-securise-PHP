# Configuration de la base de données MySQL
# =====================================================

# Hôte de la base de données (ne pas modifier pour Docker)
DB_HOST=db

# Port MySQL
DB_PORT=3306

# Nom de la base de données
DB_NAME=cash

# =====================================================
# UTILISATEUR STANDARD (Droits limités)
# =====================================================
# Utilisateur pour les opérations courantes (SELECT, INSERT, UPDATE)
# Utilisé par l'application pour les transactions normales
DB_USER=cash_user
DB_PASSWORD=user_password_secure_2024

# =====================================================
# UTILISATEUR ADMINISTRATEUR (Tous les droits)
# =====================================================
# Utilisateur pour les opérations d'administration
# Utilisé pour les opérations sensibles (DELETE, DROP, ALTER)
DB_ADMIN_USER=cash_admin
DB_ADMIN_PASSWORD=admin_password_secure_2024

# =====================================================
# UTILISATEUR ROOT (Initialisation uniquement)
# =====================================================
# Utilisé uniquement pour l'initialisation de la base
# Ne doit pas être utilisé par l'application en production
DB_ROOT_PASSWORD=rootpassword

# =====================================================
# NOTES DE SÉCURITÉ
# =====================================================
# ⚠️ IMPORTANT : En production, utilisez des mots de passe forts et uniques !
# ⚠️ Ce fichier ne doit JAMAIS être commité dans Git
# ⚠️ Modifiez tous les mots de passe avant de déployer en production
# ⚠️ L'utilisateur root ne doit être utilisé que pour l'initialisation
